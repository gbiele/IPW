\begin{tikzpicture}
	[
	 sibling distance = 4cm,
	 level distance   = 3cm,
	 every node/.style = {shape=rectangle,
		                  draw,
		                  align=center,
		                  top color = white,
	                      font=\normalsize,
                         },
    L1/.style ={sibling distance=9cm},
    L2/.style ={sibling distance=4cm},
    L3/.style ={sibling distance=1cm},
	]
	
	\newcommand{\Aa}{Is there a variable $L$\\[-1ex]
		             that is not the exposure\\[-1ex]
		             and predicts participation?}
	\newcommand{\Ba}{Bias is\\[-1ex]
		             unlikely}
	\newcommand{\Bb}{Does variable $L$ modify\\[-1ex]
		             the effect of exposure $E$\\[-1ex]
		             on the outcome $D$?}
	\newcommand{\CaEa}{Does exposure $E$ cause\\[-1ex]
		               or share a common unob-\\[-1ex]
		               served cause $U^{*}$ with $L$?}
    \newcommand{\Cb}{Is participation $P$\\[-1ex]
    	             an effect of\\[-1ex]
    	             the exposure $E$?}
    \newcommand{\Dc}{Do outcome $D$ and $L$\\[-1ex]
    	             have an unobserved\\[-1ex]
    	             common cause $U$?}	
	\node (A) {\Aa}
	child [L1] {[] node  (B1) {\Ba}}
	child [L1] {[] node  (B2) {\Bb}
		   child [L1] {node (C2) {\Cb}
		   	           child [L2] {[] node (D4) {\Ba}} 
		   	           child [L2] {[] node (D3) {\Dc}
		   	           	          child [] {[] node (E2) {\Ba} }
		   	                      child [] {[] node (E1) {\CaEa}
		   	                               child [] {[] node (F2) {MrP,AR\\[-1ex]IPW} }
		   	                               child [] {[minimum height = 10mm] node (F1) {IPW} }
   	                                       }
   	                              }
		   	            }
	   	   child [L1] {node (C1) {\CaEa}
	   	   	           child [L2] {[] node (D2) {MrP\\[-1ex]IPW}}
	   	   	           child [L2] {[minimum height = 10mm] node (D1) {IPW}}
	   	               }
		};
	
	 \begin{scope}[nodes = {draw = none}]
	   \path (A)  -- (B1) node [pos = .5, left]   {No};
	   \path (A)  -- (B2) node [pos = .5, right]  {Yes};
	   \path (B2)  -- (C1) node [pos = .5, left]   {Yes};
	   \path (B2)  -- (C2) node [pos = .5, right]  {No};
	   \path (C1)  -- (D1) node [pos = .5, left]   {Yes};
	   \path (C1)  -- (D2) node [pos = .5, right]  {No};
	   \path (C2)  -- (D3) node [pos = .5, left]   {Yes};
	   \path (C2)  -- (D4) node [pos = .5, right]  {No};
	   \path (D3)  -- (E1) node [pos = .5, left]   {Yes};
	   \path (D3)  -- (E2) node [pos = .5, right]  {No};
	   \path (E1)  -- (F1) node [pos = .5, left]   {Yes};
	   \path (E1)  -- (F2) node [pos = .5, right]  {No};
	 \end{scope}
\end{tikzpicture}